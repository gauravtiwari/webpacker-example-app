# Note: You must restart bin/webpack-dev-server for changes to take effect

default: &default
  dev_server:                 # https://webpack.js.org/configuration/dev-server/
    host: localhost           # Dev server host
    port: 8080
    https: false

  paths:                       # ~ = Rails.root
    source: app/assets/javascript     # ~/:source
    entry: entries               # ~/:source/:entry
    output: entries              # ~/public/:output
    manifest: manifest.json    # ~/public/:output/:manifest
    config: config/webpack     # ~/:config

  extensions:
    - .elm
    - .coffee
    - .js
    - .jsx
    - .ts
    - .vue
    - .sass
    - .scss
    - .css
    - .png
    - .svg
    - .gif
    - .jpeg
    - .jpg

development:
  <<: *default

test:
  <<: *default

  paths:
    output: packs-test
    manifest: manifest-test.json

production:
  <<: *default

  dev_server:
    host: 0.0.0.0 # Cloud9
    port: 8080
    https: true
